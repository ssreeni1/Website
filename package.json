{
  "name": "saneel-xyz",
  "version": "2.0.0",
  "description": "Personal website with interactive visual experience",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-posts.js",
    "dev": "npm run build && npx serve .",
    "watch": "nodemon --watch content/posts -e md --exec 'npm run build'",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "marked": "^12.0.0",
    "highlight.js": "^11.9.0",
    "nodemon": "^3.0.0",
    "@playwright/test": "^1.40.0"
  }
}
